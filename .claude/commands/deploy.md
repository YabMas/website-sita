Deploy the site to GitHub Pages. Run `./deploy.sh` from the project root. This builds the site with the GitHub Pages base URL, force-pushes dist/ contents to the `gh-pages` branch, then rebuilds for local use (`source .venv/bin/activate && python3 build.py`) so that dist/ stays usable for local preview. Report the output and confirm success.
